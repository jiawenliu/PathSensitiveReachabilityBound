[ r <- cmp_thunk_pc_a ] 0;
while [ & ( <= (pb, pc), <= (r, 0) ] 1 do {
    if [ == (r, 0) ] 2
	then { 
	   [ t <- pd] 3;
	   [ pd <- pc] 4;
	   [ pc <- t] 5;
	   [ pd <- - (pd, es) ] 6;
	}
	else { [ skip ] 7};
	[ pc <- - (pc, es)] 8
}