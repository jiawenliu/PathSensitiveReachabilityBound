
#649
while [ < (td_offset, foffset)] 0 do  {
    [ dr <- recv ] 1;
	if [ > ( + (td_offset, dr), foffset)] 2
	then { [ dw <- pwrite_1] 3 }
	else { [ dw <- pwrite_2] 4 };
	
	[td_offset <- + (td_offset, dw) ] 5;
	[bwritten <- + (bwritten, dw) ] 6

}

#960
[ i <- 0 ] 0;
while [ != (s, 0) ] 1 do {
    [i <- + (i, 1) ] 2;
	[s <- + (s, 1) ] 3;
	if [ > (i, MAXURLSIZ) ] 4
	then { [ s <- 0 ] 5 }
	else { [ skip ] 6 };
	
	[ req_url_i <- s ] 7
}
#972
[ i <- 0 ] 0;
while [ != (s, 47) ] 1 do {
    [i <- + (i, 1) ] 2;
	[s <- - (s, 1) ] 3;
	if [ > (i, MAXBUFSIZ) ] 4
	then { [ s <- 47 ] 5 }
	else { [ skip ] 6 };
	
	[ req_file_i <- s ] 7
}


#622  while (1) break;
s = rbuf;
  i = 0;
  while (1)
    {
      if (*s == '\n' && *(s - 1) == '\r' && *(s - 2) == '\n'
	  && *(s - 3) == '\r')
	{
	  s++;
	  i++;
	  break;
	}
      s++;
      i++;
    }