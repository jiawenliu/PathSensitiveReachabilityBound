int nondet();

void ex1(int m) {
  int y = 0;
  int x = m;
  while (x>=0 && y>=0) {
    if (nondet()) {
      while (y <= m && nondet())
        y++;
      x--;
    }
    y--;
  }
}

[ x <- m ] 0 ;
[ y <- 0 ] 1 ;
while [  && ( > ( x, 0 ), > ( y, 0 ) ) ] 2 do {
  [ i <- - ( m, y ) ] 3 ;
  [ x <- - ( x, 1) ] 4;
  while [  > ( i , 0) ] 5 do 
  { [ i <- - ( i, 1) ] 6  };
  [ y <- - ( m , 1 ) ] 7
}