int nondet();

void xnuSimple(unsigned int n) {
  int x = n;
  int r = 0;
  while(x > 0) {
    x = x - 1;
    r = r + 1;
    if(nondet()) {
      int p = r;
      while(p > 0)
        p--;
    r = 0;
   }
  }
}
 
[ x <- n ] 0 ;
[ r <- 0 ] 1 ;
while [  > (x , 0) ] 2 do {
  [ x <- - (x, 1) ] 3 ;
  [ r <- + (r, 1) ] 4 ;
  [ p <- r ] 5 ;
  while [  > (p , 0) ] 6 do 
  { [ p <- - (p, 1) ] 7  };
  [ r <- 0 ] 8
}