int nondet();

int foo(int n) {
 int flag = 1;
 while (flag) {
  flag = 0;
  while (n > 0 && nondet()) {
    n--;
    flag = 1;
  }
 }
 return 0;
}

//Bound according to paper: n+1
//Remark: The Example does not terminate!

[ x <- n ] 0 ;
[ flag <- 1 ] 1 ;
while [  > ( flag, 0 ) ] 2 do {
   	[ flag <- 0 ] 3;
   	[ x <- - ( x , 0) ] 4;
	while [  > ( x , n ) ] 5 do 
	{ 
		[ flag <- 1 ] 6;
   		[ x <- - ( x , 1 ) ] 7
   	}
}