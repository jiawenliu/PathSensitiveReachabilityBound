int foo(int n, int m) {
  for (int i = n; i >= 1; i = i - 1)
    for (int j = m; j >= m; j = j - 1);

  return 0;
} 

[ i <- n ] 0 ;
while [  > (i, a) ] 1 do {
	[ i <- - ( i, 1 )  ] 2;
	[ j <- m  ] 3;
while [ > (j, 0)] 4 do
{
	[ j <- - ( j, 1 )  ] 5
}
}