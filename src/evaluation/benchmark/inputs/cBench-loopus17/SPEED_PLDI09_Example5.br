int Example5(int m, int n, int dir, int fwd) {
  if(0 < m && m < n) {
    int i = m;
    while (0 < i && i < n) {
      if (dir==fwd) i++;
      else i--;
    }
  }
  return 0;
}

//Bound: max(m, n-m)


[ i <- m ] 0 ;
[ j <- - ( n, m ) ] 1 ;
while [  && ( > (i , 0), > (j , 0) ) ] 2 do {
  if [ < ( a , b ) ] 3     
  then {  [ i <- - ( i , 1 ) ] 4 }
  else {  [ j <- - ( j , 1 ) ] 5 }
}