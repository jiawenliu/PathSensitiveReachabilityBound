void start(int x, int y)
{
  int z;

  if(! (y >= 0))
    return;

  while (x>y) {
    x-=y+1;
    for (z=y; z>0; z--)
      /* nothing */;
  }
}

[ i <- - (x, y) ] 0 ;
while [  > (i, 0) ] 1 do {
	[ i <- -( - ( i, 1 ), y)  ] 2
	[ z <- y  ] 3;
	while [ > (z, 0)] 4 do
	{
		[ z <- - ( z, 1 )  ] 5
	}
}