int foo(int n, int m) {
  for (int i = n; i >= 1; i = i - 1)
    for (int j = 1; j <= m; j = j + 1);

  return 0;
}


[ i <- n ] 0 ;
while [  > (i, 0) ] 1 do {
	[ j <- m  ] 2;
	while [ > (j, 0)] 3 do
	{
		[ j <- - ( j, 1 )  ] 4
	};
	[ i <- - ( i, 1 )  ] 5
}