int foo(int a, int b, int c, int d) {
  for (int i = a; i <= b; i = i + 1)
    for (int j = c; j <= d; j = j + 1)
      for (int k = i - j; k <= i + j; k = k + 1);

  return 0;
}

[ i <- - (b, a) ] 0 ;
while [  > (i, 0) ] 1 do {
	[ j <- - (d, c)  ] 2;
	while [ > (j, 0)] 3 do
	{
		[ k <- + (j, j)  ] 4;
		while [ > (k, 0)] 5 do
		{
			[ k <- - ( k, 1 )  ] 9
		};
		[ j <- - ( j, 1 )  ] 10;
	};
	[ i <- - ( i, 1 )  ] 11
}