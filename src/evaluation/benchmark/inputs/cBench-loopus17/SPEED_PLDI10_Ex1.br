
//void ConsumeResource();

void Ex1(int n, int A[]) {
  int i = 0;
  while (i < n) {
    int j = i + 1;
    while (j < n) {
      if (A[j]) {
        //ConsumeResource();
        j--;
        n--;
     }
     j++;
   }
   i++;
  }
}

//Bound: n^2
//Reachability Bound for ConsumeResource(): n

[ i <- n ] 0;
while [ > (i, 0) ] 1 do {
    [ j <- i ] 2;
    while [ > ( j , 0 ) ] 3 do {
        [ j <- + ( j , 1 ) ] 4;
        [ i <- - ( i , 1 ) ] 5;
        [ j <- - ( j , 1 ) ] 6
    };
    [ i <- - ( i , 1 ) ] 7
 }