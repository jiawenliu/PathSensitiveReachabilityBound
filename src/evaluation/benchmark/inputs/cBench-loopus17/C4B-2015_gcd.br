/* gcd by substractions */
void gcd(int x, int y) {
  while (x > 0 && y > 0) {
    if (x > y)
      x -= y;
    else
      y -= x;
  }
}
[ i <- x ] 0 ;
[ j <- y ] 1 ;
while [  && ((> (i , 0)), (> (j , 0))) ] 2 do {
	if [ > ( i , j) ] 3     
	then {  [ i <- - (i, j) ] 4 }
	else {  [ j <- - (j, i) ] 5 }
}