int nondet();

void ex2(int m) {
  int x = m;
  int y = m;
  while (x>=2) {
    x--; y = y+x;
    while (y>=x && nondet())
	  y--;
    x--; y = y-x;
  }
}

[ x <- m ] 0 ;
[ y <- m ] 1 ;
while [  > ( x, 2 ) ] 2 do {
  [ x <- - ( x , 1)  ] 3;
  while [  > ( y , 0) ] 4 do 
  { [ y <- - ( y, 1) ] 5  };
  [ x <- - ( x , 1)  ] 6
}