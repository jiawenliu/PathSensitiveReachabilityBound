[ r <- CMP_thunk ] 0;
while [ && ( <= (pb, pc), <= (r, 0) ) ] 1 do {	
	if [ < (r, 0) ] 2
	then { 
	[ t <- pa] 3;
	[ pa <- pb] 4;
	[ pb <- t] 5;
	[ pa <- + (pa, es) ] 6
	}
	else { [ skip ] 7 };
	[ pb <- + (pb, es) ]8
}