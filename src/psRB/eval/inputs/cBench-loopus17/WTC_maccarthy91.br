[y1 <- x] 0 ;
  [y2 <- 1] 1;

  if [> (y1, 100)] 2
  then { [z <- - (y1, 10)]3 }
  else {
    while  [<= (y1, 100)]4 do {
      [y1 <-  + (y1, 11)]5 ;
      [y2 <-  + (y2, 1)]6
    }
    while  [> ( y2, 1)]7 do {
      [y1 <-  - (y1, 10)]8;
      [y2 <-  - (y2, 1) ]9;
      if  [&& ( > (y1, 100),  == (y2, 1) ) ]10
      then { [z <- - (y1, 10)] 11}
      else {
        if  [> ( y1, 100 )]12
        then {
              [y1 <-  - (y1, 10)] 13;
              [y2 <-  - (y2, 1) ] 14
              }
        else { [ skip ] 15 };
        [y1 <-  + (y1, 11)] 16;
        [y2 <-  + (y2, 1)] 17
      }
    }
  }
