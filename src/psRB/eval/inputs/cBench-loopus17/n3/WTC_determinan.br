[e <- f]0;
  [c <- d]1;
  [b <- a]2;
  [g <- h]3;
  [c <- 1]4;
  while [ && ( && ( && (&& (>= (a, + (c , 1)),  >= (c, 1)),  >= (a, c)), == (b, a)),  >= (a, 1)) ]5 do
  {
    [g <- + ( 1 , c ) ]6;
    while [  && ( && ( && (&& (&& (>= (a, + (c , 1)), >= (c, 1)), >= (+ (a, 1), g)), >= (a, g)), >= (g,  + (c , 1))), == (b, a))]7 do 
      {
        [e <- b]8;
          while [ && ( && (&& ( && (&& (&& (>= (e, c), >= (e, + (c , 1))), >= (c, 1)), >= (a, g)), >= (g, + (c , 1))), >= (a, e)),  == (b, a))]9 do 
          {
            [e <- - (e, 1)]10
          };
          [g <- + (g, 1) ]11
      };
      [c <- + (c, 1) ]12
  }